using System;

namespace _1._CSharp기초
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            // 자료의 기본 타입.
            // var : 자동으로 유추된 타입.
            var 소수점없는숫자 = 1;
            var 소수점 = 1.1;
            var 문자열 = "문자열";
            var 한글자 = '가';

            var 소수점없는숫자D = 1d;
            var 소수점F = 1.1f;
        }
    }
}